services:
  - type: web
    name: stormpc-auto-commerce
    runtime: python
    buildCommand: pip install -r requirements.txt && playwright install chromium
    startCommand: uvicorn app.main_v31:app --host 0.0.0.0 --port 8000
